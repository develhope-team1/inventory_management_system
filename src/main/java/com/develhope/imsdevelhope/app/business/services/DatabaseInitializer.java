package com.develhope.imsdevelhope.app.business.services;

/*import com.develhope.imsdevelhope.app.persistence.entities.SpringSession;
import com.develhope.imsdevelhope.app.persistence.repositories.SpringSessionRepository;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.CommandLineRunner;
import org.springframework.stereotype.Component;*/

/**
 * the database initializer.
 * @author kamar baraka.*/

/*@Component
public class DatabaseInitializer implements CommandLineRunner {

    *//*declare SpringSessionRepository dependency*//*
    private SpringSessionRepository springSessionRepository;

    @Autowired
    public DatabaseInitializer(SpringSessionRepository springSessionRepository){
        this.springSessionRepository = springSessionRepository;
    }

    @Override
    public void run(String... args) throws Exception {

        *//*construct a spring session*//*
        SpringSession springSession = new SpringSession();

        springSessionRepository.save(springSession);

    }
}*/
