package com.develhope.imsdevelhope.app.persistence.entities;

import jakarta.persistence.Column;
import jakarta.persistence.Entity;
import jakarta.persistence.Id;
import jakarta.persistence.Table;

import java.sql.Time;

/**
 * table to store the user sessions.
 * @author kamar baraka.*/

/*
@Entity
@Table(name = "SPRING_SESSION")
public class SpringSession {

    @Id
    @Column(name = "PRIMARY_ID")
    private long primaryId;

    @Column(name = "SESSION_ID")
    private long sessionId;

    @Column(name = "CREATION_TIME")
    private Time creationTime;

    @Column(name = "MAX_INACTIVE_INTERVAL")
    private Time maxInactiveInterval;

    @Column(name = "EXPIRY_TIME")
    private Time expiryTime;

    @Column(name = "PRINCIPAL_NAME")
    private String principalName;

    public long getPrimaryId() {
        return primaryId;
    }

    public void setPrimaryId(long primaryId) {
        this.primaryId = primaryId;
    }

    public long getSessionId() {
        return sessionId;
    }

    public void setSessionId(long sessionId) {
        this.sessionId = sessionId;
    }

    public Time getCreationTime() {
        return creationTime;
    }

    public void setCreationTime(Time creationTime) {
        this.creationTime = creationTime;
    }

    public Time getMaxInactiveInterval() {
        return maxInactiveInterval;
    }

    public void setMaxInactiveInterval(Time maxInactiveInterval) {
        this.maxInactiveInterval = maxInactiveInterval;
    }

    public Time getExpiryTime() {
        return expiryTime;
    }

    public void setExpiryTime(Time expiryTime) {
        this.expiryTime = expiryTime;
    }

    public String getPrincipalName() {
        return principalName;
    }

    public void setPrincipalName(String principalName) {
        this.principalName = principalName;
    }
}
*/
